package main

// all go packages will start.. (Go programming)

// pacakage main : starting point of the program
import "fmt"

func main() {
	fmt.Println("Hello, World!")

	var a int = 3
	var b int = 4
	var c = 4
	d := 5
	// only can be used inside function
	// even we don't specify the type of variable, go will infer the type
	// infer type (internal type) : go will automatically assign the type of variable

	fmt.Println(a, b, c, d)

}

// variables : memory location to store data(values)

// program? -> work with data control
